name: Build and Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install

      - name: Create .env.production
        run: |
          cat > .env.production << EOF
          DATABASE_URL=${{ secrets.DATABASE_URL }}
          UPLOAD_DIR=public/uploads
          AUTH_SECRET=${{ secrets.AUTH_SECRET }}
          NUXT_PUBLIC_VAPID_KEY=${{ secrets.NUXT_PUBLIC_VAPID_KEY }}
          NUXT_VAPID_PRIVATE_KEY=${{ secrets.NUXT_VAPID_PRIVATE_KEY }}
          NUXT_VAPID_SUBJECT=${{ secrets.NUXT_VAPID_SUBJECT }}
          EOF

      - name: Generate Prisma Client
        run: |
          pnpm prisma generate

      - name: Build application
        run: |
          export NODE_OPTIONS="--max-old-space-size=4096"
          pnpm build

      - name: Create deployment package
        run: |
          # 打包构建产物和必要的配置文件
          tar -czf output.tar.gz .output docker-compose.yml ecosystem.config.cjs

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: output-package
          path: output.tar.gz
          retention-days: 7

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: success()

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: output-package
          path: .

      - name: Test server connection
        run: |
          echo "🔍 测试服务器连接..."
          ssh -o ConnectTimeout=30 -o StrictHostKeyChecking=no \
            -i <(echo "${{ secrets.SSH_PRIVATE_KEY }}") \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} \
            -p ${{ secrets.SERVER_PORT || 22 }} \
            "echo '✅ 服务器连接成功'; hostname; whoami"

      - name: Copy file to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          source: "output.tar.gz"
          target: "/tmp/"
          timeout: 10m
          command_timeout: 5m
          strip_components: 0

      - name: Deploy on server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          timeout: 15m
          command_timeout: 10m
          script_stop: false
          script: |
            # 检查文件是否存在
            echo "🔍 检查上传的文件..."
            ls -lh /tmp/output.tar.gz || echo "❌ 文件不存在"
            echo ""

            # 配置
            APP_NAME="my-doggy-love"
            APP_DIR="/var/www/$APP_NAME"
            BACKUP_DIR="/var/backups/$APP_NAME"
            TEMP_FILE="/tmp/output.tar.gz"

            # 检查文件
            if [ ! -f "$TEMP_FILE" ]; then
              echo "❌ 错误：找不到部署包 $TEMP_FILE"
              echo "正在检查 /tmp 目录内容..."
              ls -lh /tmp/
              exit 1
            fi

            # 显示文件大小
            echo "📦 部署包大小:"
            du -h "$TEMP_FILE"
            echo ""

            # 创建必要目录
            mkdir -p "$APP_DIR"
            mkdir -p "$BACKUP_DIR"

            # 停止应用
            if command -v pm2 &> /dev/null; then
              if pm2 list | grep -q "$APP_NAME"; then
                echo "🛑 停止应用..."
                pm2 stop "$APP_NAME" 2>/dev/null || true
              fi
            fi

            # 备份旧版本
            if [ -d "$APP_DIR/.output" ]; then
              echo "💾 备份旧版本..."
              BACKUP_NAME="$BACKUP_DIR/.output.$(date +%Y%m%d_%H%M%S)"
              mv "$APP_DIR/.output" "$BACKUP_NAME"
              echo "   已备份到: $BACKUP_NAME"

              # 只保留最近 5 个备份
              ls -t "$BACKUP_DIR"/.output.* 2>/dev/null | tail -n +6 | xargs rm -rf 2>/dev/null || true
            fi

            # 解压部署包
            echo "📦 解压部署包..."
            tar -xzf "$TEMP_FILE" -C "$APP_DIR"

            # 检查解压结果
            echo "🔍 检查解压结果..."
            ls -la "$APP_DIR/"
            echo ""

            # 清理临时文件
            rm -f "$TEMP_FILE"

            # 设置权限
            echo "🔐 设置权限..."
            chmod -R 755 "$APP_DIR/.output" 2>/dev/null || echo "⚠️  权限设置跳过"

            # 清理解压时产生的其他文件（如果存在）
            rm -f "$APP_DIR/nitro.json" 2>/dev/null || true
            rm -rf "$APP_DIR/public" 2>/dev/null || true
            rm -rf "$APP_DIR/server" 2>/dev/null || true

            # 创建上传目录
            mkdir -p "$APP_DIR/public/uploads"
            chmod 755 "$APP_DIR/public/uploads"

            # 创建 PM2 日志目录
            mkdir -p "$APP_DIR/logs"
            chmod 755 "$APP_DIR/logs"

            # 创建应用环境变量文件
            echo "📝 创建应用环境变量文件..."
            echo "DATABASE_URL=${{ secrets.DATABASE_URL }}" > "$APP_DIR/.env"
            echo "UPLOAD_DIR=public/uploads" >> "$APP_DIR/.env"
            echo "AUTH_SECRET=${{ secrets.AUTH_SECRET }}" >> "$APP_DIR/.env"
            echo "NUXT_PUBLIC_VAPID_KEY=${{ secrets.NUXT_PUBLIC_VAPID_KEY }}" >> "$APP_DIR/.env"
            echo "NUXT_VAPID_PRIVATE_KEY=${{ secrets.NUXT_VAPID_PRIVATE_KEY }}" >> "$APP_DIR/.env"
            echo "NUXT_VAPID_SUBJECT=${{ secrets.NUXT_VAPID_SUBJECT }}" >> "$APP_DIR/.env"
            echo "✅ 环境变量文件已创建"

            # 启动 Docker 和 MySQL
            echo "🐳 检查 Docker 服务..."
            if ! command -v docker &> /dev/null; then
              echo "❌ Docker 未安装，请先安装 Docker"
              exit 1
            fi

            # 检查 Docker 服务是否运行
            if ! docker info &> /dev/null; then
              echo "⚠️  Docker 服务未运行，正在启动..."
              sudo systemctl start docker
              sleep 3
              if ! docker info &> /dev/null; then
                echo "❌ Docker 服务启动失败"
                exit 1
              fi
              echo "✅ Docker 服务已启动"
            else
              echo "✅ Docker 服务运行中"
            fi

            # 检查 docker-compose.yml 是否存在
            if [ -f "$APP_DIR/docker-compose.yml" ]; then
              echo "📋 检测到 docker-compose.yml，启动 MySQL 容器..."
              cd "$APP_DIR"

              # 检查 .env 文件是否存在（Docker Compose 需要）
              if [ ! -f .env ]; then
                echo "⚠️  未找到 .env 文件，创建默认配置..."
                echo "MYSQL_ROOT_PASSWORD=Lxw@199802" > .env
                echo "MYSQL_DATABASE=my_doggy_love" >> .env
                echo "MYSQL_USER=my_doggy_love_user" >> .env
                echo "MYSQL_PASSWORD=my_doggy_love_pass" >> .env
                echo "✅ .env 文件已创建"
              fi

              # 检查容器是否已经在运行
              if docker ps | grep -q my_doggy_love_mysql; then
                echo "✅ MySQL 容器已在运行"
              else
                # 尝试启动已存在的容器
                if docker ps -a | grep -q my_doggy_love_mysql; then
                  echo "🔄 启动已存在的 MySQL 容器..."
                  docker start my_doggy_love_mysql
                else
                  # 启动新的容器
                  echo "🚀 启动 MySQL 容器..."
                  docker compose up -d
                fi

                # 等待 MySQL 就绪
                echo "⏳ 等待 MySQL 就绪..."
                MAX_WAIT=60
                WAIT_COUNT=0
                while [ $WAIT_COUNT -lt $MAX_WAIT ]; do
                  if docker exec my_doggy_love_mysql mysqladmin ping -h localhost --silent 2>/dev/null; then
                    echo "✅ MySQL 已就绪"
                    break
                  fi
                  echo "   等待中... ($WAIT_COUNT/$MAX_WAIT 秒)"
                  sleep 2
                  WAIT_COUNT=`expr $WAIT_COUNT + 2`
                done

                if [ $WAIT_COUNT -ge $MAX_WAIT ]; then
                  echo "⚠️  MySQL 启动超时，但继续部署..."
                fi
              fi
            else
              echo "⚠️  未找到 docker-compose.yml，跳过 MySQL 启动"
            fi

            echo ""

            # 启动应用
            echo "🚀 启动应用..."
            cd "$APP_DIR"

            # 加载 nvm 环境（如果使用 nvm 安装 Node.js）
            if [ -f "$HOME/.nvm/nvm.sh" ]; then
              echo "📦 检测到 nvm，加载环境..."
              export NVM_DIR="$HOME/.nvm"
              [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
              echo "✅ nvm 环境已加载"
              echo ""
            fi

            # 检查 Node.js 是否已安装
            echo "🔍 检查 Node.js 安装状态..."
            if ! command -v node &> /dev/null; then
              echo "❌ Node.js 未安装"
              echo ""
              echo "请先在服务器上安装 Node.js 20："
              echo ""
              echo "使用 nvm 安装（推荐）："
              echo "  curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash"
              echo "  source ~/.bashrc"
              echo "  nvm install 20"
              echo ""
              echo "或使用官方包安装："
              echo "  curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -"
              echo "  sudo apt-get install -y nodejs"
              echo ""
              exit 1
            fi

            NODE_VERSION=$(node --version)
            NPM_VERSION=$(npm --version)
            echo "✅ Node.js 已安装: $NODE_VERSION"
            echo "✅ npm 版本: $NPM_VERSION"
            echo ""

            # 检查文件是否存在
            echo "🔍 检查应用文件..."
            ls -la .output/server/index.mjs || echo "❌ server/index.mjs 不存在"
            echo ""

            # 确保 PM2 已安装
            echo "🔍 检查 PM2 安装状态..."
            if ! command -v pm2 &> /dev/null; then
              echo "PM2 未全局安装，正在安装..."
              npm install -g pm2
              if [ $? -ne 0 ]; then
                echo "⚠️  全局安装失败，尝试使用 npx"
                PM2_CMD="npx pm2"
              else
                PM2_CMD="pm2"
                echo "✅ PM2 已全局安装"
              fi
            else
              PM2_CMD="pm2"
              echo "✅ PM2 已全局安装"
            fi

            # 显示 PM2 版本
            echo "PM2 版本:"
            $PM2_CMD --version || echo "无法获取 PM2 版本"
            echo ""

            # 检查 package.json 是否存在（PM2 生态系统需要）
            if [ ! -f "package.json" ]; then
              echo "⚠️  package.json 不存在，创建占位文件..."
              echo '{"name":"my-doggy-love","version":"1.0.0"}' > package.json
            fi

            # 删除旧进程
            echo "删除旧进程..."
            $PM2_CMD delete "$APP_NAME" 2>/dev/null || echo "无旧进程需要删除"
            echo ""

            # 启动新进程（使用 ecosystem.config.cjs 以加载环境变量）
            echo "启动 PM2 进程..."
            if [ -f "ecosystem.config.cjs" ]; then
              echo "✅ 使用 ecosystem.config.cjs 配置文件"
              $PM2_CMD start ecosystem.config.cjs
            else
              echo "⚠️  ecosystem.config.cjs 不存在，使用默认配置"
              $PM2_CMD start ".output/server/index.mjs" \
                --name "$APP_NAME" \
                --env production \
                --max-memory-restart 1G
            fi

            PM2_EXIT_CODE=$?

            if [ $PM2_EXIT_CODE -ne 0 ]; then
              echo ""
              echo "❌ PM2 启动失败 (退出码: $PM2_EXIT_CODE)"
              echo ""
              echo "查看 PM2 日志:"
              $PM2_CMD logs --lines 50 --nostream || true
              echo ""
              echo "查看 PM2 错误日志:"
              $PM2_CMD logs --err --lines 50 --nostream || true
              echo ""
              echo "尝试直接运行应用查看错误:"
              cd "$APP_DIR"
              NODE_ENV=production node .output/server/index.mjs 2>&1 | head -20 || true
              exit 1
            fi

            # 保存 PM2 配置
            $PM2_CMD save

            echo ""
            echo "✅ 应用已启动"
            echo ""
            $PM2_CMD status "$APP_NAME"
            echo ""
            echo "查看应用状态:"
            $PM2_CMD info "$APP_NAME"

            echo ""
            echo "🎉 部署完成！"

      - name: Cleanup workspace
        if: always()
        run: rm -f output.tar.gz
